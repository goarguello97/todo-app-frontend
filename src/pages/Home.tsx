import CardComponent from "../components/Card";
import Header from "../components/Header";

const Home = () => {
  return (
    <main>
      <Header />
      <CardComponent />
    </main>
  );
};

export default Home;
